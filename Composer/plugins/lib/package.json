{
  "name": "@bfc/plugins-libs",
  "version": "1.0.0",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build:botservice": "cd arm-botservice && yarn && yarn build",
    "build:appinsight": "cd arm-appinsights && yarn && yarn build",
    "build:all": "yarn build:botservice && yarn build:appinsight"
  },
  "author": "",
  "license": "ISC"
}