/**
 * `Rule` means a single element stored in the array `AdaptiveDialog.rules`.
 * Usually, a Rule may contain a series of steps.
 */
export declare const RuleEditor: {
  ({ id, data, onEvent }: { id: any; data: any; onEvent: any }): JSX.Element;
  defaultProps: {
    id: string;
    data: {};
    onEvent: () => void;
    onResize: () => void;
  };
};
//# sourceMappingURL=RuleEditor.d.ts.map
